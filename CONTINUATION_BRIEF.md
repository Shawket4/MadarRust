# Continuation brief — Madar overrides / pricing / POS sync work

> Paste this into a fresh conversation to continue. The detailed running log lives in
> the Claude auto-memory file `online-ordering-delivery-2026-06-14.md` (loaded via MEMORY.md).

You're continuing work on the **Madar ecosystem** across three repos. A lot of foundational
work is done and verified; the large online-ordering/delivery feature is still unbuilt.

## Repos
- **Backend**: `/Users/shawket/Desktop/MadarRust` — Rust/Actix/SQLx/utoipa. Git branch `audit/overnight`.
- **Dashboard**: `/Users/shawket/Desktop/MadarDashboard` — React 19 / Vite / TanStack Router v7 / Orval / Zustand.
- **POS**: `/Users/shawket/Desktop/madar_pos` — Flutter / Riverpod / Dio, offline-first (outbox).

## Conventions & gotchas (important)
- **Backend tests**: `DATABASE_URL='postgres://madar:<pw>@localhost:5433/madar_dev' cargo test --lib` — `madar` is superuser on 5433; the password is in `MadarRust/.env` (the `localhost:5433/madar_dev` line — extract it, don't echo it). `#[sqlx::test]` auto-applies migrations.
- **Money = integer piastres** everywhere; `Decimal` only mid-calc → `round_piastres`.
- **After any backend API change**: `cargo run --bin export-openapi`, then in the dashboard `npm run generate:api` (Orval reads `../MadarRust/openapi.json`).
- **Dashboard route tree** is generated by the `@tanstack/router-plugin` during vite — if you add a route file, run `npx vite build` (or dev) to regenerate `routeTree.gen.ts` **before** `npx tsc --noEmit`.
- **Dashboard verify**: `npx tsc --noEmit`, `npx eslint <files>`, `npm run build`.
- **POS verify**: `flutter analyze lib`, `flutter test`.
- **Do NOT run global `cargo fmt`** — the Rust repo isn't maintained fmt-clean; match surrounding style (clippy is clean).
- **Pre-existing POS test reds — do NOT fix here** (unrelated shift cash-continuity `edit_reason` follow-up; tracked by task chip `task_58205233`): `cart_notifier_test` + `draft_carts_notifier_test` fail to compile (`MockShiftNotifier.openShift` missing `editReason`, `shift_notifier.dart:155`), and `pending_action_test` (`PendingShiftOpen` toJson emits `edit_reason:null`). Full POS suite should be **+311 ~2 -3** — only those 3.
- **Prod DB not migrated** (matches the `order_ref` rollout state); dev is migrated.

## Done this session (all verified green)
1. **Per-branch menu overrides** (price + availability + per-size): tables `branch_menu_overrides`, `branch_menu_size_overrides`; CRUD `/branch-menu-overrides`; `GET /menu-items?branch_id=` → branch-effective price + excludes disabled; injected into `create_order` expected price.
2. **Per-branch addon overrides**: `branch_addon_overrides`; CRUD `/branch-addon-overrides`; `GET /addon-items?branch_id=`; injected into `create_order` addon price + swap base.
3. **Pricing integrity**: POS sends the full charged money breakdown (per-line `unit_price` + addon prices + `subtotal/discount_amount/tax_amount/total_amount/change_given`); backend **records verbatim**, computes an *expected* total only to **flag** deviations (`orders.price_flagged`/`price_expected_total`, `order_items.price_flagged`), **never rejects**. New fields optional with server-compute fallback. Wired through `cart.dart` / `order_api.dart` / `pending_action.dart` / `offline_queue.dart` / `checkout_sheet.dart`.
4. **Dashboard "Branch overrides"** is its own page `/menu/overrides` (sidebar nav under Menu) with **Menu items | Add-on items** sub-tabs.
5. **POS branch-effective menu + addons** (branch-keyed cache `menu:{org}:{branch}`).
6. **POS catalog auto-sync**: `OrderScreen._prefetch` force-loads on mount (= shift open) + reconnect; `Timer.periodic(10 min)` online-gated force-sync; cancelled on dispose.
7. **Server-side list params + prefetch** (replaced client load-all): `list_menu_catalog` gained `branch_id`/`overridden`/`sort`; new paginated `GET /addon-items/catalog` (kept the plain `/addon-items` array for POS); `list_bundles` gained `sort`. Overrides page + bundles page fully server-driven (search / filter / sort / paginate). Three-dots actions trigger fires `onRowPrefetch` (warms the edit dialog's `getMenuItem`); pagination-next hover prefetches the next page.

State: **358 backend lib tests green**; dashboard tsc/lint/build green; POS analyze clean.

Migrations added (dev applied): `20260614120000_branch_menu_overrides`, `…130000_branch_menu_size_overrides`, `…140000_branch_addon_overrides`.

## NOT built yet — the actual online-ordering/delivery feature
Plan: `~/.claude/plans/i-want-to-add-smooth-boot.md`. Remaining:
- `delivery_orders` entity + status workflow (Confirmed → Preparing → Ready → Out-for-delivery → Delivered).
- `branch_delivery_settings` + `delivery_zones` — concentric rings matched by **OSRM road distance** (user runs an OSRM box).
- Public ordering page — channel pre-selectable via link but switchable; outside = GPS-seeded **draggable pin + editable address**; **WhatsApp OTP** before order.
- WhatsApp send — copy `~/Downloads/apex-petroapp/src/notify.rs` gateway pattern (send-only `{phone, message}` to `WHATSAPP_SERVICE_URL`).
- **SSE → POS** push so the POS refetches a delivery-orders API.

Confirmed decisions: **cash + card on delivery** (no online gateway); zone rings by **OSRM road distance**; **WhatsApp OTP** verification; customer picks a payment method the **teller can change** at the POS; build **backend + dashboard first**, then POS realtime + WhatsApp.

## How to start
Ask what to work on next, or pick up the delivery feature's first milestone from the plan. For substantive work, fan out read-only Explore agents for discovery before editing.
