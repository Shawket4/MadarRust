# authz-gen: generates every permission artefact from ../spec/capabilities.toml.
[package]
name = "authz-gen"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
